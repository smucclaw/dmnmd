# Kept even though dmnmd is cabal-only: an old .stack-work/ is 220MB of .o files,
# and un-ignoring it to make a stray stack run visible in `git status` just risks
# committing them. (Tried that for about a minute.)
.stack-work/
dist-newstyle/
.hie/
*~
#*
.DS_Store
# A place to put stuff you don't want in git
local
# IDE config
.vscode
.idea
languages/haskell/dmnmd.iml
languages/haskell/stan.html
languages/haskell/TESTREPORT
