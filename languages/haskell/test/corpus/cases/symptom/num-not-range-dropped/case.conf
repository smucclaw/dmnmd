TITLE='`not([1..5])` silently loses the negation; a second interval is silently dropped'
WHY='The interval regex is applied with =~ as an unanchored *search*, so any surrounding text is discarded. `not([1..5])` parses as plain [1..5] — the exact logical complement of what was written. Row 2 shows the same regex silently keeping only the first of two intervals.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the FInRange regex; audit lens "numeric cell parsing", finding 6'
ARGS='--to=ts input.md'
