TITLE='`5.` is refused — right verdict, but the message names no table, column or row'
WHY='The VERDICT here is already correct: DMN 1.3 §9.2 rule 31 requires a digit after the decimal point, so `5.` is not a number. What is wrong is the diagnostic. It arrives from `mkFs` via `either error id`, so it names a source position in dmnmd rather than a position in the user`s table, which is the bar CLAUDE.md sets ("loud AND located"). Filed under symptom for the message, not the outcome; it promotes once the CellSite plumbing lands. Note the mirror-image defect: rule 31 ADMITS `.5`, which dmnmd currently refuses — see num-leading-dot-crash.'
SOURCE='src/DMN/DecisionTable.hs mkFEither :200-202, surfaced through mkFs `either error id` at :121'
ARGS='--to=ts input.md'
