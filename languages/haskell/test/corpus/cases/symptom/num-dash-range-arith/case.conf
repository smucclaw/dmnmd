TITLE='a dash-written range `40 - 50` becomes the arithmetic constant -10'
WHY='The `intersect "+-*/"` heuristic routes any cell containing a dash to the arithmetic-expression parser before any comparison arm is reached. The JS/TS guard that results drops the input variable entirely and is unconditionally truthy in JavaScript.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the `not (null ("+-*/" `intersect` arg2))` guard; audit lens "numeric cell parsing", finding 5'
ARGS='--to=ts input.md'
