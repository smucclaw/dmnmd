TITLE='a declared `: Number` column holding a non-numeric cell aborts, naming neither table nor row'
WHY='The declared type is used by columnSigs/mkFs at initial parse, before inference runs at all, so the numeric reader is handed "Fall". The diagnostic quotes the cell text but there is no file, table, column or row number; on a multi-table file that gives the author nothing to go on.'
SOURCE='src/DMN/DecisionTable.hs mkFEither mkVN via ParseTable columnSigs; audit lens "type inference", finding 20 and "table structure", finding 35'
ARGS='--to=ts input.md'
