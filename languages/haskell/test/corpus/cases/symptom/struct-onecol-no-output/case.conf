TITLE='a table with exactly one non-comment column gets NO output column'
WHY='reviseInOut guards on `length noncomments > 1`, so a one-column table keeps its single column as an INPUT and produces zero outputs. JS returns {} for every rule; the L4 backend emits a GIVETH referring to a record type that is never DECLAREd, which the real l4 typechecker rejects.'
SOURCE='src/DMN/ParseTable.hs reviseInOut; audit lens "table structure", finding 25'
ARGS='--to=ts input.md'
