TITLE='the first data row with a blank row-number is swallowed as the enum subhead row and disappears'
WHY='parseTable calls parseContinuationRows before parseDataRows, and parseContinuationRow matches on nothing more than "first cell is blank", regardless of hit policy. It uses many, so two consecutive blank-numbered rows are both swallowed and joined with unwords.'
SOURCE='src/DMN/ParseTable.hs parseContinuationRows; audit lens "table structure", finding 23'
ARGS='--to=ts input.md'
