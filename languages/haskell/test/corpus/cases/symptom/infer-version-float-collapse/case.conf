TITLE='identifier-like numbers are coerced through Float: 1.1 and 1.10 collapse into the same branch'
WHY='The inference regex ^\d+(\.\d+)?$ matches both, so the column infers Number and read :: Float gives 1.1 for both. Rows 1 and 2 become byte-identical guards; the second is dead. A Unique table that was well formed becomes a runtime conflict.'
SOURCE='src/DMN/DecisionTable.hs inferType / mkVN; audit lens "type inference", finding 16'
ARGS='--to=ts input.md'
