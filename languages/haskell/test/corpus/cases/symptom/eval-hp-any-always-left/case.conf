TITLE='hit policy A has never returned an answer: the HP_Any arm tests `not (null (nub …))` instead of `length (nub …) > 1`'
WHY='evalTable HP_Any arm fires its conflict branch whenever ANYTHING matched, so an Any table always returns Left, even for a single matching row whose outputs are trivially all-the-same. Found after the audit, not in the audit journal.'
SOURCE='src/DMN/DecisionTable.hs evalTable, the HP_Any arm'
ARGS='-q -t ts input.md'
STDIN_FILE='stdin'
