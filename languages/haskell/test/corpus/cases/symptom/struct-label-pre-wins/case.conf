TITLE='contradictory column labels: the pre-label silently wins and the post-label is discarded'
WHY='mkHeaderLabel pattern-matches the pre-label first and ignores the post-label argument entirely on those branches. A column the author explicitly marked (comment) is compiled as an output with no diagnostic, because parseLabelPre and parseLabelPost are independent optionals.'
SOURCE='src/DMN/ParseTable.hs mkHeaderLabel; audit lens "table structure", finding 34'
ARGS='--to=ts input.md'
