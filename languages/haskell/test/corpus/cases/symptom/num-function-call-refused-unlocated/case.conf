TITLE='`floor(Amount / 5)` is refused BY NAME rather than by a parser complaint'
WHY='Was a bare megaparsec `1:2: unexpected 'l'` -- a column-2 grumble about the second character of `floor`. Invocation is outside S-FEEL entirely (rules 1-38 contain no invocation production), so this refusal is the destination, not a stage: implementing it would mean a builtin roster duplicated across fNEval, the JS and PY maps, and L4. The VERDICT is now correct and the message names the construct, cites the rule and prints the repair. What is still wrong is the LOCATION: the refusal arrives through mkFs/mkF`s `either error id` and so names a source position in dmnmd rather than the table, column and row in the user`s file. CLAUDE.md`s bar is "loud AND located", and in this repo that is not a slogan -- every policy/ refusal recording is located and all the unlocated mkFs/mkF refusals are filed here. Promotes when the CellSite plumbing lands.'
SOURCE='src/DMN/ParseCell.hs callName, invocationMsg'
ARGS='--to=ts input.md'
