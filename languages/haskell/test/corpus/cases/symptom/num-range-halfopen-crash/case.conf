TITLE='a half-open range `[1..5)` is not recognised and dies at the numeric fallthrough'
WHY='The interval regex hard-codes [ and ] for both brackets. The half-open forms [e1..e2) and (e1..e2] are quoted in DecisionTable.hs own S-FEEL comment block as part of the language, but the only interval form dmnmd accepts is closed-closed with non-negative integer bounds.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the FInRange regex; audit lens "numeric cell parsing", finding 9'
ARGS='--to=ts input.md'
