TITLE='a declared `: Boolean` column aborts on `n` — the false-value list omits "f" and "n"'
WHY='Reached through the declared-type path rather than through inference, and therefore a distinct code path from infer-boolean-n-crash, but the same mkVB list defect. `yes`/`no` work fine.'
SOURCE='src/DMN/DecisionTable.hs mkFEither mkVB; audit lens "table structure", finding 36'
ARGS='--to=ts input.md'
