TITLE='suffix `5 <=` compiles to `> 5` — the comparison is inverted and the boundary is excluded'
WHY='The suffix arms are the negation of the prefix arms rather than their mirror. `5 <= Age` means Age >= 5; dmnmd emits Age > 5, so the boundary value 5 stops matching.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the `"<=" isSuffixOf` arm; audit lens "numeric cell parsing", finding 1'
ARGS='--to=ts input.md'
