TITLE='Haskell `read` is more permissive than FEEL: `0x10` is silently accepted as 16'
WHY='The numeric fallthrough is Haskell readMaybe :: Float, whose grammar is not FEEL. A hexadecimal literal is not S-FEEL and should be rejected; instead it silently becomes 16.0.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, mkVN readMaybe; audit lens "numeric cell parsing", finding 11'
ARGS='--to=ts input.md'
