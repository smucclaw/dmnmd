TITLE='a negative threshold `>= -5` aborts the run'
WHY='The `intersect "+-*/"` arithmetic heuristic sits before every comparison arm, so the minus sign captures the whole cell and the arithmetic parser then chokes on the leading `>`. No comparison against a negative number is expressible anywhere in dmnmd.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, guard ordering; audit lens "numeric cell parsing", finding 10'
ARGS='--to=ts input.md'
