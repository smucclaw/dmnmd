TITLE='one non-numeric cell demotes the whole numeric column, turning `<= 8` into the literal string "<= 8"'
WHY='inferTypes computes {Number, String} for the column, the length==1 guard fails, vartype stays Nothing, reprocessRows declines, and every comparison in the column evaporates into string equality. The only reporting was a Debug.Trace that is commented out. Exit 0, no warning on either channel.'
SOURCE='src/DMN/DecisionTable.hs inferTypes / reprocessRows; audit lens "type inference", finding 13'
ARGS='--to=ts input.md'
