TITLE='prose containing `..` is inferred as a NUMBER column and then dies at the numeric reader'
WHY='inferType tests the cell against the unanchored PCRE list ["^\d+(\.\d+)?$", "\.\.", ">", "<", "="], so any occurrence anywhere in the value wins. An ordinary label column of prose is typed Number and every cell then fails to read.'
SOURCE='src/DMN/DecisionTable.hs inferType, the unanchored regex list; audit lens "type inference", finding 15'
ARGS='--to=ts input.md'
