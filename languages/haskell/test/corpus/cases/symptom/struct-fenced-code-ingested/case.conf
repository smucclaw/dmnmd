TITLE='a pipe table inside a fenced code block is ingested as a real decision table'
WHY='irrelevantLine has no notion of code fences, so ``` is just another non-pipe line and the fenced table is chunked and compiled, inheriting the name of the last enclosing heading. This matters because README.md is itself a live fixture, so documentation-only tables become code.'
SOURCE='app/ParseMarkdown.hs irrelevantLine; audit lens "table structure", finding 31'
ARGS='--to=ts input.md'
