TITLE='a range with decimal bounds `[1.5..2.5]` is not recognised and dies at the numeric fallthrough'
WHY='The interval regex hard-codes (\d+) for both bounds, so decimal bounds never match and the whole cell text is handed to the numeric literal reader. `[1.5..2.5]` is a legal S-FEEL interval.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the FInRange regex; audit lens "numeric cell parsing", finding 9'
ARGS='--to=ts input.md'
