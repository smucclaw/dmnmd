TITLE='two headings that clean to the same name: --pick selects BOTH, and the emitted JS is a SyntaxError'
WHY='Main.hs filters with `tableName dt elem picks`, a membership test rather than a lookup, so --pick Rates matches both tables and both are emitted into the same stream as `export function Rates`. Any two headings differing only after a colon collapse to the same name.'
SOURCE='app/Main.hs pick filter and app/ParseMarkdown.hs cleanTableName; audit lens "table structure", finding 30'
ARGS='--to=ts --pick Rates input.md'
