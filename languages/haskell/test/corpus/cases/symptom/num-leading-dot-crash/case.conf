TITLE='the numeric fallthrough is unguarded: `.5` has no arm and dies there'
WHY='Every numeric cell shape the guard chain does not recognise lands on the bare literal reader. The message names the cell text but still no file, table, column or row. Confirmed in the audit for `.5`, `5.`, `1_000`, `5%`, `[1..5)` and `5 >`.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the `otherwise` arm and mkVN; audit lens "numeric cell parsing", finding 11'
ARGS='--to=ts input.md'
