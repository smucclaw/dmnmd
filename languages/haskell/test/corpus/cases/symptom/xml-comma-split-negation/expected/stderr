input.dmn: warning: decision "Dish": <informationRequirement> "InformationRequirement_season" declares a <requiredInput> on "#InputData_season", which dmnmd does not model. The decision requirement graph is dropped: each <decision> becomes an independent function. The <inputData> node is not modelled as such; its value arrives here as an ordinary parameter, and any typeRef on its <variable> is not applied.
input.dmn: warning: table "Dish": input column "season": rule "Rule_1": "not(\"Fall\", \"Winter\", \"Spring\", \"Summer\")" is not a FEEL string literal, nor a comma-separated list of them. dmnmd keeps the text as written — quotes included — and splits it on commas, which is wrong for anything more structured than a literal.
