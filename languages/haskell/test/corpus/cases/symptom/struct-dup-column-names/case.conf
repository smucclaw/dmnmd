TITLE='duplicate column names are accepted, producing an unsatisfiable guard and a JS SyntaxError'
WHY='parseHeaderRow does no uniqueness check, so the emitted guard is `Season === "Fall" && Season === "Wet"`, always false, and node rejects the duplicate parameter outright. evalTable still works because matching is positional, so the interpreter and the transpiler disagree about the same table.'
SOURCE='src/DMN/ParseTable.hs parseHeaderRow; audit lens "table structure", finding 33'
ARGS='--to=ts input.md'
