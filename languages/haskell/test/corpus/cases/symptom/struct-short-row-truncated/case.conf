TITLE='a short data row silently loses its guards and its output'
WHY='zipWith truncates to the shorter list at three separate places (mkFEELCol, getInputs/getOutputs, and matches). The missing guard is therefore not merely wildcarded, it is not emitted at all, so the rule fires for ANY value of that input; the missing output silently becomes empty. Nothing warns.'
SOURCE='src/DMN/ParseTable.hs mkFEELCol and getInputs/getOutputs; audit lens "table structure", finding 27'
ARGS='--to=ts input.md'
