TITLE='a data row whose first cell starts with `-` is silently discarded as a horizontal rule'
WHY='parseDThr matches the literal prefix "| -" and then eats the rest of the line as a separator. Maximally hostile because `-` is dmnmd own wildcard marker, so writing `-` in the row-number column to mean "unnumbered" is a natural thing to do. Exit 0, no warning.'
SOURCE='src/DMN/ParseTable.hs parseDThr; audit lens "table structure", finding 22'
ARGS='--to=ts input.md'
