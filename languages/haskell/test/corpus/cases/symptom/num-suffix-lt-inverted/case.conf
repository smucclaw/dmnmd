TITLE='suffix `5 <` compiles to `>= 5` — the boundary is wrongly included'
WHY='`5 < Age` means Age > 5. dmnmd emits Age >= 5, so 5 matches when it must not. This is exactly the mapping the `<=` suffix arm should have had, and vice versa.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the `"<" isSuffixOf` arm; audit lens "numeric cell parsing", finding 2'
ARGS='--to=ts input.md'
