TITLE='the hit policy is parsed and then silently ignored by the js/py backends'
WHY='C+ parses to HP_Collect Collect_Sum and evalTable correctly sums to 15, but the js/py backends emit the same first-match if/else-if chain they emit for U, so the generated function returns 10. No warning. The L4 backend is the only one that refuses. Known and noted in CLAUDE.md; recorded here because the failure mode is silent divergence between `dmnmd -q` and `dmnmd --to=js` on the same file.'
SOURCE='src/DMN/Translate/JS.hs and PY.hs ignore hitpolicy; audit lens "table structure", finding 38'
ARGS='--to=ts input.md'
