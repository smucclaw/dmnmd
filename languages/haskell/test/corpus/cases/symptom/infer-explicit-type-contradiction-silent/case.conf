TITLE='an explicit header type that contradicts the column wins in silence, and the comparisons evaporate'
WHY='inferTypes detects that the declared type differs from the inferred one and returns the header unchanged; the only reporting was a Debug.Trace that is commented out. reprocessRows then declines to touch a String column, so `<= 8` becomes the literal string "<= 8". Exit 0, no warning.'
SOURCE='src/DMN/DecisionTable.hs inferTypes, the vartype-disagreement branch; audit lens "type inference", finding 20'
ARGS='--to=ts input.md'
