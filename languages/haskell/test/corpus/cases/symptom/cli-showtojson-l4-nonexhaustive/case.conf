TITLE='the interactive evaluator with `-t l4` hits a non-exhaustive pattern in showToJSON'
WHY='showToJSON in app/Main.hs has arms for Js, Ts and Py but not L4, so `dmnmd -q -t l4` dies with a Non-exhaustive patterns error the moment a query succeeds. Found after the audit, not in the audit journal.'
SOURCE='app/Main.hs showToJSON'
ARGS='-q -t l4 input.md'
STDIN_FILE='stdin'
