TITLE='hit policy CA leaks the same Debug.Trace line to stderr, alongside a correct result'
WHY='CA returns 5, 10 and 20 — every matching row, which is right — and prints
`outputs has length 3` to stderr, which is not. Same defect and same fix as
symptom/eval-collect-cnt-debug-trace; both arms carry the trace. Kept as a
separate case because CA and C# return different shapes (a list of rows versus a
single count), so a rewrite could plausibly fix one and break the other.'
SOURCE='src/DMN/DecisionTable.hs evalTable, HP_Collect Collect_All arm'
ARGS='-q -t ts input.md'
STDIN_FILE='stdin'
