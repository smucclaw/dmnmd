error: input.md: table "R": column "Grade": row 1: value outside the column's declared domain {C A, B} — the cell reads A
error: input.md: table "R": column "Grade": row 3: value outside the column's declared domain {C A, B} — the cell reads C
dmnmd: one or more decision tables in input.md could not be read
