TITLE='reviseInOut picks the output column by structural equality, not position, so a duplicated name turns TWO columns into outputs'
WHY='ColHeader derives Eq over (label, varname, vartype, enums), so every column equal to the rightmost one is flipped to output. An input column is silently converted into an output, and the emitted JS object literal then has a duplicate key.'
SOURCE='src/DMN/ParseTable.hs reviseInOut; audit lens "table structure", finding 24'
ARGS='--to=ts input.md'
