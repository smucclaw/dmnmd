TITLE='`0o17` is accepted as 15, but FEEL has no octal literal'
WHY='Same root cause as num-nonsfeel-exponent-accepted: Haskell`s `read` accepts octal and hexadecimal, DMN 1.3 §9.2 rule 31 accepts neither. The hex spelling is already recorded as num-hex-accepted; this is its octal sibling, and it is recorded separately because a fix that anchors the grammar must close both and nothing currently proves it does.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the `readMaybe :: Maybe Float` fallthrough at :200-202'
ARGS='--to=ts input.md'
