TITLE='a thousands separator turns `>= 1,000` into "at least 1, or exactly 0"'
WHY='mkFsEither splits every cell on "," into a multi-value OR before mkFEither ever sees it, so `>= 1,000` becomes the two alternatives ">= 1" and "000". A fee threshold silently becomes an almost-always-true guard, and exits 0.'
SOURCE='src/DMN/DecisionTable.hs mkFsEither, the splitOn ","; audit lens "numeric cell parsing", finding 4'
ARGS='--to=ts input.md'
