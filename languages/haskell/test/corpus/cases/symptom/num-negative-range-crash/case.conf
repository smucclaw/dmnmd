TITLE='a range with a negative bound `[-5..5]` aborts the run'
WHY='Same guard-ordering defect as num-negative-threshold-crash, reached through a different surface syntax: the minus inside the interval trips the arithmetic heuristic before the interval regex is consulted.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, guard ordering; audit lens "numeric cell parsing", finding 10'
ARGS='--to=ts input.md'
