TITLE='prose containing `>` is inferred as a NUMBER column and then dies at the numeric reader'
WHY='Same unanchored inferType regex list as infer-prose-with-dots-crash, through a different alternative. A hierarchy label like `L1 > L2` is ordinary prose.'
SOURCE='src/DMN/DecisionTable.hs inferType, the unanchored regex list; audit lens "type inference", finding 15'
ARGS='--to=ts input.md'
