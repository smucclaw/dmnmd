TITLE='prose containing `>` is inferred as a NUMBER column and then dies at the numeric reader'
WHY='Same unanchored inferType substring test as infer-prose-with-dots-crash, through a different alternative. A hierarchy label like `L1 > L2` is ordinary prose. The refusal is now located (`table "Hierarchy": column "Path": row 1`), which does not help: the location is right and the TYPE is wrong, so this stays a symptom.'
SOURCE='src/DMN/DecisionTable.hs inferType, the unanchored substring test; audit lens "type inference", finding 15'
ARGS='--to=ts input.md'
