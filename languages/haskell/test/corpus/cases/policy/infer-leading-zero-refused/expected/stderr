error: input.md: table "ProductCode": column "Code": row 1: the cell reads "007" — a leading zero has no numeric meaning, so this is either the number 7 written oddly or an identifier that happens to be digits, and dmnmd will not choose. Declare the column: "Code : String" keeps "007" exactly as written; "Code : Number" reads it as 7.
error: input.md: table "ProductCode": column "Code": row 2: the cell reads "042" — a leading zero has no numeric meaning, so this is either the number 42 written oddly or an identifier that happens to be digits, and dmnmd will not choose. Declare the column: "Code : String" keeps "042" exactly as written; "Code : Number" reads it as 42.
dmnmd: 2 decision table(s) in input.md could not be read
