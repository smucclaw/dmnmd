TITLE='a table with two output columns emits a multi-key object from the JS/TS backend'
WHY='No case in the corpus had more than one output column, so the entire
multi-output path was invisible. Here the two outputs also have different
inferred types — Dish a string, Fee a number — so the recording pins that the
columns keep their own types rather than being unified.'
SOURCE='src/DMN/Translate/JS.hs; src/DMN/DecisionTable.hs reviseInOut'
ARGS='--to=ts input.md'
