TITLE='hit policy R returns every matching row, in rule-number order'
WHY='The control for eval-hp-first: identical table, identical input, different
hit policy, and the answer must differ. R returns kids/Spareribs/Stew in that
order. Ordering is the whole content of the policy — `sortOn row_number` — so a
rewrite that shuffled rows would be invisible without this.'
SOURCE='src/DMN/DecisionTable.hs evalTable, HP_RuleOrder arm'
ARGS='-q -t ts input.md'
STDIN_FILE='stdin'
