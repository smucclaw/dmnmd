TITLE='the same sub-header refusal down the INFERRED path, distinguished only by source position'
WHY='The twin of policy/num-subheader-declared-refused, and deliberately built to be indistinguishable from it in every way but one. Here the column is not declared, so the sub-header cells survive pass 1 as strings; inferTypes then types the column Number from the data row, and retypeEnums re-runs the declared domain through reprocessRows, where mkFAt refuses 0x10. Same table name, same column name, same absent row number, same message text to the byte. The ONLY difference in the recording is the source position: mkFsAt in the twin, mkFAt here. That is precisely the property test/corpus/README.md invokes to justify keeping positions in the recordings, and until this pair existed the argument rested on two cases whose table and column names happened to differ. Pinned: both paths refuse, both exit 1, both emit nothing, both locate to table and column with no row segment, and the two remain tellable apart.'
SOURCE='src/DMN/DecisionTable.hs retypeEnums -> reprocessRows -> mkFAt with siteRow = Nothing'
ARGS='--to=ts input.md'
