TITLE='the same sub-header refusal down the INFERRED path, distinguished only by source position'
WHY='The twin of policy/num-subheader-declared-refused. Here the column is not declared, so the sub-header cells survive pass 1 as strings; inferTypes then types the column Number from the data row, and retypeEnums re-runs the declared domain through reprocessRows, where mkFAt refuses 0x10. Same table name, same column name, same absent row number, same message text to the byte.

What this case pins on its own: that input is refused, with that message, at exit 1, emitting nothing, located to table and column with no row segment.

What it does NOT pin, contrary to what this WHY used to say: that the two remain "tellable apart". It claimed the recordings differed only by source position and that this was "precisely the property test/corpus/README.md invokes to justify keeping positions". RETRACTED (D-7). run-corpus.sh scrubs positions before classifying a diff, so a swap from mkFAt to mkFsAt was reported as cosmetic and exited 0 -- the argument this case was built to hold up never held. After D-7 there is no CallStack in either recording. The two cases remain two cases because their INPUTS differ (declared : Number vs inferred), and the wrapper distinction is enforced where it can be: test/Spec.hs, "located cell refusals (mkFsAt / mkFAt)".'
SOURCE='src/DMN/DecisionTable.hs retypeEnums -> reprocessRows -> mkFAt with siteRow = Nothing'
ARGS='--to=ts input.md'
