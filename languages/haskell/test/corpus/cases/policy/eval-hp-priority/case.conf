TITLE='hit policy P returns the highest-priority match, ranked by the output enum order'
WHY='Priority is the one hit policy whose answer depends on the enum subhead row
rather than on row order. All three rows match Guests=3; the enum row ranks
Stew first, so P must return Stew — deliberately NOT row 1, so that a rewrite
which quietly degraded P into F would change this recording. CLAUDE.md notes
the L4 backend reorders arms through the same `outputOrder` function, so this
also guards the shared helper.'
SOURCE='src/DMN/DecisionTable.hs evalTable HP_Priority arm; outputOrder/mySort'
ARGS='-q -t ts input.md'
STDIN_FILE='stdin'
