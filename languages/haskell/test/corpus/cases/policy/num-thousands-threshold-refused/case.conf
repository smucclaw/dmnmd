TITLE='`>= 1,000` is refused as thousands-shaped, and the refusal is located'
WHY='The threshold spelling of policy/num-thousands-refused -- read that one first, including its retraction and the note that this is a deliberate deviation from a conformant reading. This is the more alarming of the two. The old output was `(Amount >= 1.0 || Amount === 0.0)`, a guard that fires for almost every input, in a column whose author plainly meant at least one thousand: conformant, silent, and catastrophically not what was meant. The refusal names the repair both ways, drop the separator or add a space if two alternatives really were intended. Pinned: exit 1, nothing emitted, and the location `table "CommaThreshold": column "Amount": row 1`. The escape hatch stays open and is pinned separately by policy/num-thousands-with-space-accepted.'
SOURCE='src/DMN/ParseCell.hs thousandsGrouped; src/DMN/DecisionTable.hs mkFsEither, thousandsMsg; located by mkFsAt'
ARGS='--to=ts input.md'
