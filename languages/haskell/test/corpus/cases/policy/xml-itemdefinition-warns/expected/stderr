input.dmn: warning: <itemDefinition> "tScores": it is isCollection="true", a list type. dmnmd has a 'DMN_List' type but its CELL layer does not honour it: 'DMN.DecisionTable.mkFEither' delegates a list type straight to its base type, so every cell in the column is read as a scalar. A markdown column declared '[Number]' already shows what that produces — L4 'GIVEN tags IS A LIST OF NUMBER' with a guard 'tags EQUALS 5', which does not typecheck, and TypeScript 'tags: number[]' compared with '=== 5.0', which is never true (symptom/md-list-column-cells-are-scalars). Mapping isCollection onto that would turn this refusal into a silent wrong answer, so the column is refused instead.
