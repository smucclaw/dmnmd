input.dmn: warning: <itemDefinition> "tDriver": it is a structured type (<itemComponent> "Name", "Points"). dmnmd's decision table columns are scalars, so there is nothing to map a record onto. A column whose typeRef names this is refused.
