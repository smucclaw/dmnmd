input.dmn: warning: decision "Band": <informationRequirement> "InformationRequirement_age" declares a <requiredInput> on "#InputData_age", which dmnmd does not model. The decision requirement graph is dropped: each <decision> becomes an independent function. The <inputData> node is not modelled as such; its value arrives here as an ordinary parameter, and any typeRef on its <variable> is not applied.
input.dmn: error: table "Band": row 1 and row 2 have the same input cells (age: < 18), so row 2 can never match: a table with hit policy Unique must not contain overlapping rules (DMN 1.3 §8.2.10). Delete one of the two rows, or change an input cell of row 2 so that the two rules select different inputs. dmnmd compares the values the cells denote, not the text: two cells spelled differently for the same value (1.1 and 1.10, say) are the same guard.
dmnmd: input.dmn: one or more decision tables were refused (see the errors above); nothing was emitted for them.
