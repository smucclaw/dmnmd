dmnmd: error: table "Junk": column "tags": row 1: the cell reads "not(RED" — a collection column's cell must be a plain member value or "-". dmnmd does not implement FEEL function calls, negation or list literals in a decision-table cell, and the comma split has already broken this one into fragments. If the value really is that text, quote the whole cell.
error: table "Junk": column "tags": row 1: the cell reads "BLUE)" — a collection column's cell must be a plain member value or "-". dmnmd does not implement FEEL function calls, negation or list literals in a decision-table cell, and the comma split has already broken this one into fragments. If the value really is that text, quote the whole cell.
CallStack (from HasCallStack):
  error, called at src/DMN/DecisionTable.hs:415:15 in dmnmd:DMN.DecisionTable
  error, called at src/DMN/DecisionTable.hs:415:15 in dmnmd:DMN.DecisionTable


