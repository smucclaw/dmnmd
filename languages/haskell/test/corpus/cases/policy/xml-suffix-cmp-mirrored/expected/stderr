dmnmd: warning: table "Threshold": row 2 has "-" in every input column, so it is this table's answer for any input no other rule matches. Under hit policy Unique that must not be a rule — it would overlap every other rule (§8.2.10) — so it is emitted as the table's default output value (§8.2.11) instead, and the rule count drops by one. The meaning is preserved for every engine; the row's own number is not, because a default has no rule number.
