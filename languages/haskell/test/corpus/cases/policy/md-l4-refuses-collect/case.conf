TITLE='the L4 backend refuses a Collect table rather than silently dropping matches'
WHY='Collect/Aggregate/OutputOrder/RuleOrder are list-valued; a scalar BRANCH would silently drop matches, so L4.hs errors out deliberately. That refusal is intended behaviour and must survive. Contrast symptom/eval-hitpolicy-ignored-by-js, where js/py do NOT refuse.'
SOURCE='src/DMN/Translate/L4.hs, the deliberate error for list-valued hit policies'
ARGS='--to=l4 input.md'
