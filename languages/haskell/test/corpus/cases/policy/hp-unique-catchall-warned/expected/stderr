dmnmd: warning: table "Dish": row 3 has "-" in every input column, so it matches everything and overlaps every other rule; a table with hit policy Unique must not contain overlapping rules (§8.2.10). dmnmd reads it as you meant it — matching is first-match — but under Unique another engine may evaluate the rules in any order and return this row instead of a more specific one. DMN spells this as a default output value (§8.2.11) rather than as a rule, which dmnmd cannot yet carry. For a portable document give the table hit policy F or P, which permit overlapping rules.
