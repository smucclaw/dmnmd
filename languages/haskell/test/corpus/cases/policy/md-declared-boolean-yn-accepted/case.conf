TITLE='a DECLARED `: Boolean` column accepts y/n too -- the same fix down the other code path'
WHY='The declared twin of md-boolean-yn-accepted, and its whole reason for existing separately: this reaches mkVB from mkFsAt in pass 1, where no inference runs at all, rather than from reprocessRows in pass 2. Keeping both pins that the declared path and the inferred path agree about the vocabulary. Before the fix the two recordings differed only in a CallStack line number.'
SOURCE='src/DMN/DecisionTable.hs mkFEither mkVB, the false-value list'
ARGS='--to=ts input.md'
