TITLE='the JS backend emits the props-style function it is supposed to'
WHY='A backend smoke case, so a rewrite that changes the IR without meaning to change the rendering is caught.'
SOURCE='src/DMN/Translate/JS.hs'
ARGS='--to=js -r input.md'
