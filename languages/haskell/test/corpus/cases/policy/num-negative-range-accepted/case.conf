TITLE='`[-5..5]` is an interval with a negative lower bound'
WHY='Same root cause as policy/num-negative-threshold-accepted, plus the old interval recogniser hard-coded `(\d+)` for both bounds and so could not have matched a sign even if it had been reached. Now the bounds are rule-31 literals like any other.'
SOURCE='src/DMN/ParseCell.hs interval, numericLiteral'
ARGS='--to=ts input.md'
