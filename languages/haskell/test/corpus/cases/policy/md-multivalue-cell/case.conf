TITLE='a comma-separated multi-value cell is parsed as an OR of alternatives'
WHY='This is the intended meaning of the comma, and it is why the thousands-separator symptom cases exist. The OR semantics must survive the rewrite even though the tokenisation must change.'
SOURCE='src/DMN/DecisionTable.hs mkFsEither, the splitOn ","'
ARGS='--to=ts input.md'
