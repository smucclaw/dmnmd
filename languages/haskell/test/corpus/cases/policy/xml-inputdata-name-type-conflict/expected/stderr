dmnmd: warning: input column "x" appears with more than one type (Number, String) across tables "A", "B". A DMN document has ONE <inputData> per name and every decision references it, so the emitted <variable> can carry only one typeRef while each <inputExpression> keeps its own — the document will say the column is one type and use it as another. Markdown has no global scope, so this identification is the emitter's, not the author's. Rename one of the columns if they are not the same thing.
