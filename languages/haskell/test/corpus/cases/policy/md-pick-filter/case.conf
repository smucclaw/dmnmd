TITLE='--pick selects a named table by its cleaned heading name'
WHY='Table naming and selection is how README.md doubles as a live fixture. symptom/struct-tablename-collision records the case where the membership test misbehaves; this records the case where it works.'
SOURCE='app/Main.hs pick filter; app/ParseMarkdown.hs cleanTableName'
ARGS='--to=ts --pick Second input.md'
