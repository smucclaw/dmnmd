TITLE='hit policy C< returns the minimum of the output column across matching rows'
WHY='Same table and input as eval-hp-collect-sum, so the three Collect cases
differ only in the operator and must give three different answers (35 / 5 / 20).
That mutual distinctness is the point: it catches a rewrite that wired every
Collect operator to the same aggregation. See also
symptom/eval-collect-min-empty-crash for what this arm does with no matches.'
SOURCE='src/DMN/DecisionTable.hs evalTable, HP_Collect Collect_Min arm'
ARGS='-q -t ts input.md'
STDIN_FILE='stdin'
