TITLE='prefix comparisons < <= > >= are parsed correctly'
WHY='The prefix arms are the ones that are right today. `< 18` must stay `Age < 18.0`, and the strictness must not flip. This is the direct control for the four suffix cases under symptom/.'
SOURCE='src/DMN/DecisionTable.hs mkFEither, the four isPrefixOf arms'
ARGS='--to=ts input.md'
