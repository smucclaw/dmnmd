TITLE='prose containing `..` is a STRING column: an anchored parse, not a substring search'
WHY='Fixed by anchoring, and it is the half of D-2 that refuses LESS, not more. inferType used to ask whether a cell CONTAINED "..", ">", "<", "=" or a spaced arithmetic operator, so one occurrence anywhere in the value typed the whole column Number and every cell then failed at the numeric reader: exit 1, on an ordinary label column of prose. `Coming soon...` does not parse as a number, a comparison, an interval or arithmetic, so it is not numeric evidence, so the column is String and the table simply works.'
SOURCE='src/DMN/DecisionTable.hs inferEvidence, the parseNumberCell oracle; DECISIONS.md D-2'
ARGS='--to=ts input.md'
