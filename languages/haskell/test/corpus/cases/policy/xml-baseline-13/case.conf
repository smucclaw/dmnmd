TITLE='a valid DMN 1.3 document with one decision table converts and exits 0'
WHY='The known-good DMN 1.3 file. It is here rather than only in DmnXmlSpec because the XML reader builds its cells through the same mkFEither the grammar rewrite replaces, so a cell-language change can break XML import without touching any markdown test.'
SOURCE='src/DMN/XML/ParseDMN.hs; src/DMN/XML/XmlToDmnmd.hs'
ARGS='-f xml --to=ts input.dmn'
