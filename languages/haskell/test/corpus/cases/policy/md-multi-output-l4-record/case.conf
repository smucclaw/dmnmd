TITLE='the same two-output table compiles to an L4 DECLARE record plus a mk<Name> constructor'
WHY='CLAUDE.md states multi-output tables get "a DECLARE record plus a mk<Name>
constructor"; nothing in the corpus held it to that. Same input as
policy/md-multi-output-columns so the two backends can be compared directly on
one table. The DECLARE block also fixes the field order and the L4 type names,
which is where a change in type inference would surface first.'
SOURCE='src/DMN/Translate/L4.hs; BUILD-SPEC-dmnmd-to-l4.md'
ARGS='--to=l4 input.md'
