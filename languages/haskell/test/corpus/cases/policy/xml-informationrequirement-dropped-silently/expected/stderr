input.dmn: warning: decision "Band": <informationRequirement> "InformationRequirement_age" declares a <requiredInput> on "#InputData_age", which dmnmd does not model. The decision requirement graph is dropped: each <decision> becomes an independent function. The <inputData> node is not modelled as such; its value arrives here as an ordinary parameter, and any typeRef on its <variable> is not applied.
input.dmn: warning: decision "Fare": <informationRequirement> "InformationRequirement_band" declares a <requiredDecision> on "#Decision_band", which dmnmd does not model. The decision requirement graph is dropped: each <decision> becomes an independent function. Nothing calls the required decision; its output arrives here as an ordinary parameter, so run that decision first and pass its result in.
