error: input.md: table "GuestCount": column "Guest Count": dmnmd cannot infer a type for this column: row 1 "<= 8" reads as Number, and row 3 "unknown" reads as String. An undeclared column has to agree with itself, and dmnmd will not pick a winner and read the losing cells as literal text — which is what it used to do, silently. Declare the column: "Guest Count : String" reads every cell as the text the author wrote; "Guest Count : Number" requires every cell to be a number, a comparison, an interval or arithmetic.
dmnmd: 1 decision table(s) in input.md could not be read
