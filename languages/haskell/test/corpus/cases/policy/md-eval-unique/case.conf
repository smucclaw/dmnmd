TITLE='the interpreter evaluates a Unique table and returns the matching row output'
WHY='The end-to-end evaluation path, which is what the transpilers are supposed to agree with. Exercised through -q so the corpus covers the interpreter, not only the backends. Note the explicit -t ts: bare `dmnmd -q` crashes, see symptom/cli-showtojson-unknown-format.'
SOURCE='src/DMN/DecisionTable.hs evalTable, HP_Unique arm'
ARGS='-q -t ts input.md'
STDIN_FILE='stdin'
