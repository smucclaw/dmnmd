TITLE='`5 >` mirrors to `< 5` -- the arm that did not exist at all'
WHY='See policy/num-suffix-lte-mirrored. The old chain had suffix arms for `<=`, `<` and `>=` but NONE for `>`, so this cell fell through to the numeric literal parser and died. Writing the four arms as one mirroring function rather than four guards is what makes the missing case impossible to reintroduce.'
SOURCE='src/DMN/ParseCell.hs suffixCmp'
ARGS='--to=ts input.md'
