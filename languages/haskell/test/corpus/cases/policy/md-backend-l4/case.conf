TITLE='the L4 backend emits GIVEN/GIVETH and a first-match BRANCH with a synthesized OTHERWISE'
WHY='The L4 backend is the most constrained one and the one with a design spec (BUILD-SPEC-dmnmd-to-l4.md). Its ditto grid is column-alignment-critical, so byte-level output matters here more than anywhere else.'
SOURCE='src/DMN/Translate/L4.hs'
ARGS='--to=l4 input.md'
