TITLE='a Unique table whose rows overlap reports a conflict rather than picking one'
WHY='Reporting the conflict IS the hit policy. A rewrite that quietly returns the first match would turn a detectable authoring error into a silent wrong answer, which is the whole class of defect this corpus exists to prevent.'
SOURCE='src/DMN/DecisionTable.hs evalTable, HP_Unique arm'
ARGS='-q -t ts input.md'
STDIN_FILE='stdin'
