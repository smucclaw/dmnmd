input.dmn: error: table "Paint": column "shade": dmnmd cannot infer a type for this column: row 1 "5" reads as Number, and row 2 "Blue" reads as String. An undeclared column has to agree with itself, and dmnmd will not pick a winner and read the losing cells as literal text — which is what it used to do, silently. Declare the column: "shade : String" reads every cell as the text the author wrote; "shade : Number" requires every cell to be a number, a comparison, an interval or arithmetic.
dmnmd: input.dmn: one or more decision tables were refused (see the errors above); nothing was emitted for them.
