dmnmd: input.dmn: this document declares DMN 1.5 and dmnmd cannot represent it faithfully.
  decision "AllAdult": <every> is a boxed quantifier (every / in / satisfies), added in DMN 1.4. dmnmd does not model it.
dmnmd models decision tables: a <decision> must hold a <decisionTable>, and an <itemDefinition> may carry <allowedValues> but not <typeConstraint>.

