input.dmn: error: table "Access": input column "roles": typeRef "tRoles" names an <itemDefinition> with isCollection="true" whose element type is <typeRef>Any</typeRef>, which places no restriction on the elements. dmnmd infers a column's type from its cells and can only infer a scalar, so there is nothing here to make the ELEMENT type — and reading the column as a scalar would turn every membership cell into an equality test against the whole collection, which can never match. Name the element type (for example <typeRef>string</typeRef>). Refusing to convert this table.
dmnmd: input.dmn: one or more decision tables were refused (see the errors above); nothing was emitted for them.
